# Local Django configuration
SECRET_KEY=change-me-for-local-development
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Google OAuth: basic identity scopes only (openid email profile)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional demo-only market-data mode. Never use this to represent live prices.
DEMO_DATA=false
